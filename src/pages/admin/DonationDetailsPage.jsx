import React from 'react';
import { useParams } from 'react-router-dom';
import { ShieldCheck } from 'lucide-react';
import { PageContent } from '@/components/common/PageContent';
import { PageHeader } from '@/components/common/PageHeader';
import { Card } from '@/components/cards/Card';
import { AdminDetailSection, AdminStatus, BackLink, DetailValue } from '@/components/admin';
import { getAdminDonationById } from '@/constants/adminData';
export default function DonationDetailsPage() { const { donationId } = useParams(); const donation = getAdminDonationById(donationId); const steps = ['Posted', 'Claimed', 'Pickup Pending', 'Collected', 'Completed']; const currentIndex = donation.status === 'AVAILABLE' ? 0 : donation.status === 'CLAIMED' ? 1 : donation.status === 'PICKUP_PENDING' ? 2 : donation.status === 'COLLECTED' ? 3 : donation.status === 'COMPLETED' ? 4 : 0; return <PageContent><PageHeader breadcrumbs={<BackLink to="/admin/donations">Back to Donations</BackLink>} title={donation.name} description={`${donation.id} · ${donation.quantity}`} badge={<AdminStatus status={donation.status} />} /><div className="grid gap-6 xl:grid-cols-[1.15fr_0.85fr]"><div className="space-y-6"><AdminDetailSection title="Donation overview"><dl className="grid gap-3 sm:grid-cols-2"><DetailValue label="Food name" value={donation.name} /><DetailValue label="Quantity" value={donation.quantity} /><DetailValue label="Category" value={donation.category} /><DetailValue label="Dietary profile" value={donation.dietary} /><DetailValue label="Prepared time" value={donation.preparedAt} /><DetailValue label="Consume before" value={donation.consumeBefore} /><DetailValue label="Storage condition" value={donation.storage} /><DetailValue label="Allergens" value={donation.allergens} /></dl><p className="mt-4 rounded-lg border border-border bg-surface-muted/50 p-3 text-xs leading-relaxed text-text-secondary">{donation.notes}</p></AdminDetailSection><AdminDetailSection title="Donor information"><dl className="grid gap-3 sm:grid-cols-2"><DetailValue label="Organization" value={donation.donor} /><DetailValue label="Contact" value={donation.donorId} /><DetailValue label="Location" value={donation.location} /><DetailValue label="Posted" value={donation.posted} /></dl></AdminDetailSection><AdminDetailSection title="Pickup information"><dl className="grid gap-3 sm:grid-cols-2"><DetailValue label="Pickup location" value={donation.pickupLocation} /><DetailValue label="Pickup window" value={donation.pickupWindow} /><DetailValue label="Claimed by" value={donation.claimant} /><DetailValue label="Pickup status" value={donation.status === 'AVAILABLE' ? 'Awaiting claim' : donation.status} /></dl></AdminDetailSection></div><div className="space-y-6"><Card padding="lg"><div className="flex items-center gap-2"><ShieldCheck className="h-5 w-5 text-primary" /><h2 className="text-base font-bold text-text-primary">Donation timeline</h2></div><div className="mt-6 space-y-5">{steps.map((step, index) => <div key={step} className="flex gap-3"><span className={`flex h-7 w-7 shrink-0 items-center justify-center rounded-full ${index <= currentIndex ? 'bg-primary text-white' : 'bg-surface-muted text-text-muted border border-border'}`}><span className="text-xs font-bold">{index + 1}</span></span><div><p className={`text-sm font-semibold ${index <= currentIndex ? 'text-text-primary' : 'text-text-muted'}`}>{step}</p><p className="mt-0.5 text-xs text-text-secondary">{index <= currentIndex ? (index === 0 ? donation.posted : donation.lastUpdated) : 'Awaiting next transition'}</p></div></div>)}</div></Card><Card variant="subtle" padding="lg"><p className="text-xs font-semibold uppercase tracking-wider text-text-muted">Administrative information</p><dl className="mt-4 grid gap-3"><DetailValue label="Current status" value={donation.status} /><DetailValue label="Created date" value={donation.posted} /><DetailValue label="Last updated" value={donation.lastUpdated} /></dl></Card></div></div></PageContent>; }
