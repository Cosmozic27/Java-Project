import React, { useState } from 'react';
import { Building2, Mail, MapPin, Phone, UserRound } from 'lucide-react';
import { PageContent } from '@/components/common/PageContent';
import { PageHeader } from '@/components/common/PageHeader';
import { Button } from '@/components/common/Button';
import { Card } from '@/components/cards/Card';
import { Modal } from '@/components/feedback/Modal';
import { useToast } from '@/components/feedback';
import { NGO_PROFILE } from '@/constants/ngoData';

export function ProfilePage() {
  const [open, setOpen] = useState(false); const toast = useToast();
  return <PageContent><PageHeader title="Organization Profile" description="Keep the organization details that donors and receiving partners use to understand your collection setup." actions={<Button variant="outline" onClick={() => setOpen(true)} leftIcon={<Building2 className="h-4 w-4" aria-hidden="true" />}>Edit Profile</Button>} /><div className="grid gap-6 xl:grid-cols-[minmax(280px,0.7fr)_minmax(0,1.3fr)]"><Card padding="lg" className="text-center"><span className="mx-auto flex h-20 w-20 items-center justify-center rounded-full border border-border bg-primary-light text-2xl font-bold text-primary-dark">HK</span><h2 className="mt-5 text-xl font-bold text-text-primary">{NGO_PROFILE.name}</h2><p className="mt-1 text-sm text-text-secondary">{NGO_PROFILE.type}</p><span className="mt-4 inline-flex items-center rounded-full border border-primary/20 bg-primary-light px-2.5 py-0.5 text-xs font-semibold text-primary-dark">NGO workspace</span><p className="mt-6 border-t border-border/70 pt-5 text-xs text-text-secondary">Member since {NGO_PROFILE.joined}</p></Card><Card padding="lg"><h2 className="text-base font-bold text-text-primary">Organization details</h2><p className="mt-1 text-xs text-text-secondary">Illustrative profile information for this frontend prototype.</p><dl className="mt-5 grid gap-4 sm:grid-cols-2"><div className="rounded-lg border border-border bg-surface-muted/50 p-4"><dt className="flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-wider text-text-muted"><UserRound className="h-3.5 w-3.5" aria-hidden="true" />Contact person</dt><dd className="mt-2 text-sm font-semibold text-text-primary">{NGO_PROFILE.contactPerson}</dd></div><div className="rounded-lg border border-border bg-surface-muted/50 p-4"><dt className="flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-wider text-text-muted"><Building2 className="h-3.5 w-3.5" aria-hidden="true" />Organization type</dt><dd className="mt-2 text-sm font-semibold text-text-primary">{NGO_PROFILE.type}</dd></div><div className="rounded-lg border border-border bg-surface-muted/50 p-4"><dt className="flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-wider text-text-muted"><Mail className="h-3.5 w-3.5" aria-hidden="true" />Official email</dt><dd className="mt-2 text-sm font-semibold text-text-primary">{NGO_PROFILE.email}</dd></div><div className="rounded-lg border border-border bg-surface-muted/50 p-4"><dt className="flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-wider text-text-muted"><Phone className="h-3.5 w-3.5" aria-hidden="true" />Phone</dt><dd className="mt-2 text-sm font-semibold text-text-primary">{NGO_PROFILE.phone}</dd></div><div className="rounded-lg border border-border bg-surface-muted/50 p-4 sm:col-span-2"><dt className="flex items-center gap-1.5 text-[11px] font-semibold uppercase tracking-wider text-text-muted"><MapPin className="h-3.5 w-3.5" aria-hidden="true" />Service area & pickup address</dt><dd className="mt-2 text-sm font-semibold text-text-primary">{NGO_PROFILE.serviceArea}<br /><span className="font-normal text-text-secondary">{NGO_PROFILE.address}</span></dd></div><div className="rounded-lg border border-border bg-surface-muted/50 p-4 sm:col-span-2"><dt className="text-[11px] font-semibold uppercase tracking-wider text-text-muted">Pickup notes</dt><dd className="mt-2 text-sm leading-relaxed text-text-primary">{NGO_PROFILE.pickupNotes}</dd></div><div className="rounded-lg border border-border bg-surface-muted/50 p-4 sm:col-span-2"><dt className="text-[11px] font-semibold uppercase tracking-wider text-text-muted">Organization description</dt><dd className="mt-2 text-sm leading-relaxed text-text-secondary">{NGO_PROFILE.description}</dd></div></dl></Card></div><Modal isOpen={open} onClose={() => setOpen(false)} title="Edit organization profile" description="Profile persistence is not connected yet." footer={<><Button variant="outline" onClick={() => setOpen(false)}>Cancel</Button><Button variant="primary" onClick={() => { setOpen(false); toast.info('Profile edits are local prototype UI only and were not saved.', 'Prototype action'); }}>Save locally</Button></>}><p className="text-sm leading-relaxed text-text-secondary">This prototype modal demonstrates the future editing flow. No API, database, or permanent profile update is connected.</p></Modal></PageContent>;
}
export default ProfilePage;
